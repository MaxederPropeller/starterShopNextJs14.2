/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'dummyapp.com', 'firebasestorage.googleapis.com', 'unspalsh.com', 'images.unsplash.com'],

    },

};

export default nextConfig;
